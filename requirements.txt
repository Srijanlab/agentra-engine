# Vercel installs from here. Kept minimal — no uvicorn (Vercel provides the
# ASGI server), no playwright/graphifyy/texttospeech (loop-only / dashboard
# nicety), to stay under the 250 MB function limit.
claude-agent-sdk>=0.2.110
pyyaml>=6.0
fastapi>=0.115
pyjwt[crypto]>=2.9
httpx>=0.27
google-cloud-firestore>=2.19
