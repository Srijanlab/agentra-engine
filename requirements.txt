# Vercel installs from here (see vercel.json installCommand, which also prunes
# claude-agent-sdk's 199 MB bundled CLI — the engine never runs it). No uvicorn
# (Vercel provides the ASGI server), no playwright/graphifyy/texttospeech.
claude-agent-sdk>=0.2.110
pyyaml>=6.0
fastapi>=0.115
pyjwt[crypto]>=2.9
httpx>=0.27
google-cloud-firestore>=2.19
