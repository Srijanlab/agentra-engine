# Vercel installs from here. The engine is API + state only -- no cycle agents,
# no Claude Agent SDK, no uvicorn (Vercel provides the ASGI server).
pyyaml>=6.0
fastapi>=0.115
pyjwt[crypto]>=2.9
httpx>=0.27
google-auth[requests]>=2.30
langfuse>=4.0
boto3>=1.35
