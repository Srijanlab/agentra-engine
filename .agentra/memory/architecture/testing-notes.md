No CI pipeline exists yet (no .github/workflows). Two live-integration tests exist under tests/ (test_generic_spawn_integration.py, test_safety_integration.py) but are NOT run automatically -- they need real CLAUDE_CODE_OAUTH_TOKEN/ANTHROPIC_API_KEY credentials and cost real money, matching this repo's own established convention (see their docstrings). For a normal code change, `python3 -m py_compile <file>` plus manually exercising the affected code path (e.g. a local `agentra serve` run) is the closest thing to a test suite right now -- treat 'no test coverage for X' as a legitimate, real gap worth fixing, not a false alarm.